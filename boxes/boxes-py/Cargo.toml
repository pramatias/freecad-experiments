[package]
name = "boxes-py"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.25.0", features = ["extension-module"] }
boxes-core = { path = "../boxes-core" }
boxes-geo  = { path = "../boxes-geo" }
