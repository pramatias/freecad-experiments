[build-system]
requires      = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name    = "leroy"
version = "0.1.0"                   # ← was missing

[tool.maturin]
manifest-path = "leroy/Cargo.toml"
