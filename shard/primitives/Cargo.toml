
[package]
name    = "primitives"
version = "0.1.0"
edition = "2021"

[lib]
name       = "primitives"
crate-type = ["rlib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
nalgebra = "0.33"
